{
  "name": "electron-markdwon-editor",
  "productName": "EM",
  "version": "1.0.0",
  "description": "markdown editor, support for export pdf and html",
  "main": "main.js",
  "build": {

  },
  "scripts": {
    "start": "cross-env NODE_ENV=development electron .",
    "pre-pack": "rimraf ./release-builds",
    "package-mac": "npm run pre-pack && electron-packager . --overwrite --platform=darwin --arch=x64 --icon=assets/icons/mac/icon.icns --prune=true --out=release-builds",
    "package-win": "npm run pre-pack && electron-packager . electron-tutorial-app --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Todo List App\"",
    "package-linux": "npm run pre-pack && electron-packager . electron-tutorial-app --overwrite --asar=true --platform=linux --arch=x64 --icon=assets/icons/png/icon.png --prune=true --out=release-builds",
    "build-mac": "electron-builder"
  },
  "author": "YUE LI",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^5.2.0",
    "electron": "^5.0.3",
    "electron-builder": "^20.43.0",
    "electron-packager": "^13.1.1",
    "rimraf": "^2.6.3"
  },
  "dependencies": {
    "highlight.js": "^9.15.8",
    "markdown-pdf": "^9.0.0",
    "marked": "^0.6.2"
  }
}
